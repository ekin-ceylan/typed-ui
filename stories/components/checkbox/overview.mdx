import { Meta, Canvas } from '@storybook/addon-docs/blocks';
import * as CheckboxStories from './check-box.stories.js';

<Meta of={CheckboxStories} name="Overview" />

# CheckBox

Bu sayfa, `check-box` bileşeninin kısa kullanımını ve Storybook kontrollerini gösteren minimal bir MDX örneği.

## HTML kullanımı (örnek)

```html
<check-box field-id="subscribe" label="Abone Ol">Okudum, Anladım</check-box>
```

<Canvas of={CheckboxStories.Playground} />
